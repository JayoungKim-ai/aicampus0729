const a = 0;
const b = 1;
console.log(a && b);
console.log(a || b);

const isAdmin = true;
console.log(isAdmin && "<button>관리자버튼</button>");

const newMessage = 0;
console.log(newMessage && "<p>새로운 메시지가 있습니다.</p>");
